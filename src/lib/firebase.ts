// Firebase has been removed. This file is a stub to prevent any remaining import errors.
// All data is now stored in MongoDB via /api/* routes.
export const db = null;
export const auth = null;
export const storage = null;
export default null;
